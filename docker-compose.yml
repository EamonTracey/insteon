services:
  falmouth-agent:
    build: .
    ports:
      - "443:443"
    environment:
      - MCP_HOST=http://host.docker.internal:8000/sse
      - OLLAMA_HOST=http://host.docker.internal:11434
      - PYTHONUNBUFFERED=1
